{
  "private": true,
  "scripts": {
    "build": "nitro build",
    "dev": "nitro dev",
    "prepare": "nitro prepare",
    "preview": "node .output/server/index.mjs",
    "test": "vitest"
  },
  "devDependencies": {
    "nitropack": "latest",
    "vitest": "^1.4.0"
  }
}
